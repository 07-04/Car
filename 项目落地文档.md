# 肉牛养殖场巡检机器人 — 项目落地文档

> 更新：Phase 1 由「ESP32 + STM32 双板」简化为 **单 ESP32-S3**（BLE 通信 + 电机控制合一），STM32 后移为 Phase 4 可选（编码器闭环控速）。

### 1.1 架构

#### 最终目标（三层）

```
┌─────────────────────────────────────────────────────┐
│  Jetson Orin Nano (P3766)  ← 上层 AI 大脑（Phase 2）│
│  · 摄像头（IMX219/USB）· 视觉模型（YOLO 牛只识别）   │
│  · 红外融合 · 路径规划 · ROS · 数据记录              │
└───────────────────────┬─────────────────────────────┘
                        │ USB 串口 / UART（指令 + 数据）
┌───────────────────────▼─────────────────────────────┐
│  STM32F103  ← 中层实时执行（Phase 4 可选）           │
│  · 编码器闭环控速 · 更多硬件定时器/编码器接口          │
└───────────────────────┬─────────────────────────────┘
                        │ UART
┌───────────────────────▼─────────────────────────────┐
│  ESP32-S3  ← 通信 + 执行（Phase 1 实际）             │
│  · BLE 遥控 · 电机控制 · 传感器采集                   │
└─────────────────────────────────────────────────────┘
```

#### Phase 1 实际落地（单 ESP32-S3）

```
手机 BLE App ──BLE──> ESP32-S3 ──> TB6612 D24A ──> 电机 ×4
```

- **ESP32-S3-N16R8**（16MB Flash + 8MB PSRAM）单板承担全部 Phase 1 工作：BLE 通信 + 电机 PWM 控制。
- 去掉 STM32 中间层，BLE 收到的字符直接驱动电机，无需串口转发。
- 固件：`firmware\esp32_motor\esp32_motor.ino`。

### 1.2 硬件拓扑与数据流

```
手机(BLE App) ──BLE──> ESP32-S3 ──GPIO/PWM──> TB6612 D24A ──> 电机
                        │
                        └── 后接：HC-SR04 / SHT30 / MLX90614（传感器）
```

### 1.3 通信协议（Phase 1，BLE）

| 链路 | 接口 | 方向 | 内容 |
|------|------|------|------|
| 手机 ↔ ESP32-S3 | BLE（Nordic UART Service）| 双向 | 运动指令、状态回传 |

**BLE 参数**：
- 设备名：`RobotCar`
- Service UUID：`6E400001-B5A3-F393-E0A9-E50E24DCCA9E`
- RX 特征值（手机 → ESP32，可写）：`6E400003-B5A3-F393-E0A9-E50E24DCCA9E`
- TX 特征值（ESP32 → 手机，Notify）：`6E400002-B5A3-F393-E0A9-E50E24DCCA9E`

**运动指令（ASCII，一次发一个字符）**：

```
"F" 前进  "B" 后退  "L" 左转  "R" 右转  "S" 停
"A" 自主避障（自动前进，超声波+红外避障）  "S" 或任意方向键退出避障
"1"~"9" 设速度（1=30 ... 9=110，默认 70）
```

> ⚠️ App 发送格式必须切 **ASCII / 文本**，不能是 HEX（否则「F」会被当成十六进制报错）。若保持 HEX，字符 `F` 对应 `46`。

### 1.4 引脚分配表（Phase 1，ESP32-S3）

| 功能 | 引脚 | 说明 |
|------|------|------|
| 左组电机 PWM | GPIO4 | 接 D24A PWMA+PWMD（并联）|
| 左组方向 IN1 | GPIO5 | 接 D24A AIN1+DIN1（并联）|
| 左组方向 IN2 | GPIO6 | 接 D24A AIN2+DIN2（并联）|
| 右组电机 PWM | GPIO7 | 接 D24A PWMB+PWMC（并联）|
| 右组方向 IN1 | GPIO8 | 接 D24A BIN1+CIN1（并联）|
| 右组方向 IN2 | GPIO9 | 接 D24A BIN2+CIN2（并联）|
| 供电 | VIN / GND | D24A 板载 5V / 公共地 |
| STBY 使能 | 3V3 | 接 D24A STBY（必须，否则电机不转）|
| 烧录 | USB（CH340）| 电脑认 `COM3` |
| 传感器 | GPIO10~14, 16~18, 21 | 见下方传感器引脚表 |

> **左组 = 左前(D) + 左后(A)**，**右组 = 右前(C) + 右后(B)**。四履带差速转向，只有左右两个独立控制单元。
>
> **D24A 控制脚成对并联**：`PWMA+PWMD`→GPIO4、`PWMB+PWMC`→GPIO7、`AIN1+DIN1`→GPIO5、`AIN2+DIN2`→GPIO6、`BIN1+CIN1`→GPIO8、`BIN2+CIN2`→GPIO9。
>
> **HC-SR04 用 3.3V 供电直连**（VCC→3V3），Echo 输出 3.3V，不用分压电阻。KY-032 也用 3.3V 供电（OUT 输出 3.3V 才安全）。若改用 5V 供电，Echo/OUT 都要串分压。

**传感器引脚表（避障 + 环境）**：

| 传感器 | VCC | 信号 | ESP32 GPIO |
|--------|-----|------|-----------|
| HC-SR04 前超声 | 3V3 | Trig / Echo | GPIO10 / GPIO11 |
| KY-032 左红外 | 3V3 | OUT | GPIO12 |
| KY-032 右红外 | 3V3 | OUT | GPIO13 |
| SG90 舵机 | 5V（D24A）| Signal | GPIO14 |
| BH1750 光强 | 3V3 | SCL / SDA | GPIO16 / GPIO17（ADDR→GND）|
| SHT31 串口温湿度 | 3V3 | TX / RX | GPIO18 / GPIO21（交叉接）|

> KY-032 有障碍时 OUT 输出**低电平**，代码判断 `digitalRead==LOW` 即障碍。模块上蓝色电位器调检测距离。

### 1.5 分阶段实施路线

| 阶段 | 目标 | 关键交付物 | 依赖 |
|------|------|-----------|------|
| **Phase 1** | 底盘跑通 + BLE 遥控 | ESP32 固件（BLE+电机）、结构件（3D 打印）、接线完成 | 物料到位 |
| **Phase 2** | AI 接入 | Jetson 环境、摄像头驱动、YOLO 视觉模型 | Jetson 到位 |
| **Phase 3** | 红外测温融合 | MLX90614 数据融合、体温预警逻辑 | Phase 1 完成 |
| **Phase 4** | 自主巡检闭环 | 视觉引导 + 路径规划 + 任务调度（可选加 STM32 编码器闭环控速）| Phase 2+3 完成 |

> 结构件直接采用金属焊接（不锈钢/镀锌钢），跳过 3D 打印。

### 1.6 完整接线总表（单 ESP32 方案）

> 接线顺序：先接【供电 + D24A + 4 电机 + ESP32 控制】让电机动 → 再接传感器。
>
> **线材速查**：电池是红黑两根裸线，直接接 D24A `VIN`/`GND`；「电机 → D24A 输出口」用 22AWG（电机自带线）；其余全部用杜邦线。

#### ① 供电（全部共地）

```
12V 电池 (E351S, 5100mAh, 红黑两根线, 充放电同口)
  ├─ 红线 (+) ──→ D24A VIN
  └─ 黑线 (−) ──→ D24A GND（公共地）

D24A 板载 5V ──→ ESP32-S3 VIN(5V)
D24A GND ──→ ESP32-S3 GND
ESP32-S3 3V3 ──→ D24A STBY（使能，必须接）
```

> **电池红黑别接反**：红线=12V+，黑线=GND。D24A 有防反接保护，接反不烧但不供电，对调即可。
>
> ⚠️ **充放电同口**：充电时用「DC 母座转红黑线」转接头，**充电前必须先把电池红黑线从 D24A 断开**，不能边充边用。
>
> ⚠️ 所有 GND 连一起：电池黑线、D24A GND、ESP32 GND。
>
> **电池没开关，D24A 板载电源开关（SS-12D1）就是整机总开关**。ESP32 由 D24A 板载 5V 供电，关掉 D24A 开关即整机断电。

#### ② D24A 驱动板

电源（3 根）：
| D24A | 接 | 线 |
|------|-----|-----|
| `VIN` | 电池红线（12V+）| 电池自带线 / AWG18 |
| `GND` | 电池黑线（12V−）+ 公共地 | 电池自带线 / AWG18 |
| `STBY` | ESP32 3V3 | 杜邦线 |

4 个电机（每个接口是 6 针排针，电机 2 根线接【第 2 针 OUT1】+【第 5 针 OUT2】）：
| 电机 | D24A 输出口 | 电机线接 | 线 |
|------|------------|---------|-----|
| 左后 | `A` 口 | AOUT1 + AOUT2 | 22AWG |
| 右后 | `B` 口 | BOUT1 + BOUT2 | 22AWG |
| 右前 | `C` 口 | COUT1 + COUT2 | 22AWG |
| 左前 | `D` 口 | DOUT1 + DOUT2 | 22AWG |

> 6 针排针排列：`[GND][OUT1][E1A][E1B][OUT2][3v3]`。电机线接第 2 针 + 第 5 针，其余空着。哪根接 OUT1/OUT2 无所谓，反了单电机反转，对调即可。

控制线（ESP32 → D24A，成对并联，杜邦线）：
| ESP32-S3 | D24A | 线 |
|-------|------|-----|
| `GPIO4` | PWMA + PWMD（左组）| 杜邦线 |
| `GPIO5` | AIN1 + DIN1 | 杜邦线 |
| `GPIO6` | AIN2 + DIN2 | 杜邦线 |
| `GPIO7` | PWMB + PWMC（右组）| 杜邦线 |
| `GPIO8` | BIN1 + CIN1 | 杜邦线 |
| `GPIO9` | BIN2 + CIN2 | 杜邦线 |
| `GND` | GND（共地）| 杜邦线 |

> `+` 表示一根线「一分二」并联两个脚：例如 GPIO4 同时接 PWMA 和 PWMD（左前、左后两电机同信号）。

#### ③ 传感器（已接，避障 + 环境）

超声波 + 红外避障（全部杜邦线，3.3V 供电直连无分压）：
| 传感器 | VCC | GND | 信号 | ESP32 GPIO |
|--------|-----|-----|------|-----------|
| HC-SR04 前超声 | 3V3 | GND | Trig / Echo | GPIO10 / GPIO11 |
| KY-032 左红外 | 3V3 | GND | OUT（EN→3V3）| GPIO12 |
| KY-032 右红外 | 3V3 | GND | OUT（EN→3V3）| GPIO13 |

环境传感器（已接，本次未写固件）：
| 器件 | VCC | 信号 | ESP32 GPIO |
|------|-----|------|-----------|
| SG90 舵机 | 5V（D24A）| Signal | GPIO14 |
| BH1750 光强 | 3V3 | SCL / SDA | GPIO16 / GPIO17（ADDR→GND）|
| SHT31 串口温湿度 | 3V3 | TX / RX | GPIO18 / GPIO21（交叉接）|

> 传感器引脚避开 GPIO4~9（电机）及 PSRAM 占用脚 GPIO33~37。HC-SR04 / KY-032 用 3.3V 供电可直连；若改 5V 供电，Echo / OUT 要串分压电阻。

#### ④ 容易踩的坑

1. STBY 不接 3V3 → 电机不转（最容易漏）
2. ESP32 与 D24A 不共地 → 信号不通
3. 电池红线/黑线接反 → 不通电（有防反接，对调即可）
4. 电机线接反不烧，只单电机反向，对调即可
5. HC-SR04 / KY-032 用 5V 供电时 Echo / OUT 直连会烧 GPIO → 必须 3.3V 供电或串分压

### 1.7 自主避障逻辑

固件：`firmware\esp32_motor\esp32_motor.ino`。BLE 发 `A` 进入自主避障，`S` 或方向键退出。

传感器：前方 HC-SR04 测距（阈值 25cm），左右 KY-032 红外（有障碍输出低电平）。

决策（每 50ms 循环一次）：

| 前方距离 | 左红外 | 右红外 | 动作 |
|---------|--------|--------|------|
| < 25cm | 挡 | 挡 | 后退左转（死胡同逃出）|
| < 25cm | 挡 | 空 | 右转 |
| < 25cm | 空 | 挡/空 | 左转 |
| ≥ 25cm | 挡 | 空 | 右偏（避开左侧）|
| ≥ 25cm | 空 | 挡 | 左偏（避开右侧）|
| ≥ 25cm | 空/挡 | 空/挡 | 直行（都空直行，都挡走窄缝）|

> 调参：代码里 `avoidDist`（默认 25cm）改触发距离；KY-032 蓝色电位器调红外检测距离；速度用 BLE `1`~`9` 设。

---

## 2. 物料清单（BOM）

### 2.1 已有物料

| 名称 | 型号 | 数量 | 用途 |
|------|------|------|------|
| 直流减速电机 | JGA25-370 12V 1:30（输出轴 4mm D 轴）| 4 | 轮式四驱（原履带电机 XCR380SM18225 已弃用）|
| 橡胶轮 | 65mm（轴孔 4mm D 孔）| 4 | 驱动轮 |
| 电机支架 | JGA25-370 L 型金属座 | 4 | 电机固定到平台板 |
| 电机驱动 | TB6612 四路 D24A（WHEELTEC 带稳压版）| 1 | 四路驱动四个电机 |
| 通信+执行主控 | ESP32-S3-N16R8 | 1 | BLE 遥控 + 电机控制（Phase 1 单板）|
| 底层主控（可选）| STM32F103（RBT6 128KB）| 1 | Phase 4 编码器闭环控速（Phase 1 不用）|
| 电源模块 | MB-102 | 1 | 备用（未使用）|
| 锂电池 | 12V 5100mAh（WHEELTEC E351S，红黑两根线）| 1 | 主供电（无开关，靠 D24A 开关控制）|
| 降压模块 | LM2596（12V→5V）| 1 | 备用（Phase 3 若红外读数抖动再启用）|
| 粗电源线 | AWG18 红黑 | 各 1 米 | 电池→驱动主供电 |

### 2.2 新增物料 — Phase 1

| 名称 | 型号 | 数量 | 用途 |
|------|------|------|------|
| 超声波传感器 | HC-SR04 | 1 | 前方避障测距 |
| 红外避障 | KY-032 | 2 | 左/右避障 |
| 光强传感器 | BH1750 | 1 | 环境光照 |
| 温湿度 | SHT31（串口 TTL）| 1 | 空气温湿度 |
| 舵机 | SG90 | 1 | 探头/云台转动 |
| 红外测温 | MLX90614 | 1 | 非接触测目标温度（Phase 3）|
| DC 母座转接线 | DC 5.5×2.1 母座转红黑线 | 1 | 充电用 |
| 杜邦线 | 母对母 | 若干 | 信号连接 |
| 杜邦线 | 公对母 / 公对公 | 各 10 根 | 传感器/扩展 |
| 螺丝 | M4 + 螺母 | 16 套 | 固定平台板 |
| 铜柱/尼龙柱 | M3 | 若干 | 固定传感器、主控 |

### 2.3 新增物料 — Phase 2

| 名称 | 型号 | 数量 | 用途 |
|------|------|------|------|
| AI 主控 | Jetson Orin Nano 开发套件 | 1 | 视觉模型推理 |
| 摄像头 | IMX219（CSI）或 USB 摄像头 | 1 | Jetson 视觉输入 |
| 无线模块 | WiFi 模块（若套件未带）| 1 | 视频回传 |
| （可选）热成像 | MLX90640（32×24）| 1 | 远程筛查发烧牛只 |

---

## 3. 机械结构设计

### 3.1 底盘布局（四轮差速）

左右两侧各 2 个电机轮并联成一组

```
        前
  ┌─────────────┐
  │ 左前  │ 右前 │      左前+左后 = 左组（同向同速）
  │      │      │      右前+右后 = 右组（同向同速）
  │ 左后  │ 右后 │      差速转向：左组反转 + 右组正转 = 原地左转
  └─────────────┘
        后
```

整车尺寸：

| 参数 | 数值 |
|------|------|
| 车总长 | ≈ 300 mm |
| 车总宽 | ≈ 230 mm |
| 平台高度 | ≈ 55 mm |

驱动轮 + 电机：

| 参数 | 数值 |
|------|------|
| 电机 | JGA25-370 12V 1:30，输出轴 4mm D 轴 |
| 轮径 | 65 mm |
| 电机接线 | 2 根 |
| 安装 | L 型金属支架 + M3 螺丝，孔位待电机到手后定 |

### 3.2 平台板设计

| 参数 | 数值 | 说明 |
|------|------|------|
| 长 | 300 mm | 前后贯穿 |
| 宽 | 120 mm | 左右 |
| 厚 | 6 mm | M4 锁紧强度充足 |
| 孔数量 | 8 个 | 每个单元 2 个耳朵孔 |
| 孔直径 | 4.5 mm | M4 通过孔 |
| 左列 X | 5 mm | 离左边缘 5mm |
| 右列 X | 115 mm | 离右边缘 5mm |
| 孔 Y 坐标 | 25 / 110 / 195 / 280 mm | 前单元 25+110，后单元 195+280，孔距 85mm |

图纸：巡检机器人平台板.scad。

### 3.3 传感器与摄像头安装方案

| 器件 | 安装位置 | 固定方式 |
|------|---------|---------|
| HC-SR04 ×3 | 前/左/右三面，朝外侧水平 | 角钢/钣金支架 + 尼龙柱 |
| SHT30 | 平台上方通风处，避免阳光直射 | 支架 + 尼龙柱 |
| MLX90614 | 车头前方，斜向下对准目标 | 支架，预留俯仰调节 |
| IMX219（Phase2）| 车头前方高处 | 摄像头支架 |
| ESP32-S3 | 平台中央 | 尼龙柱架高，避开发热 |
| 电池 | 平台中央，重心居中 | 电池固定件/魔术贴 |

### 3.4 结构件清单（金属焊接）

| 结构件 | 数量 | 方案 |
|--------|------|------|
| 平台板 | 1 | 120×300×6mm，8 个 M4 孔，不锈钢/镀锌钢板折弯或焊接 |
| HC-SR04 支架 | 3 | 角钢/钣金折弯 |
| 摄像头支架 | 1 | 钣金折弯，可调俯仰 |
| 传感器集成支架 | 1 | 钣金 |
| 电池固定件 | 1~2 | 钣金抱箍/扎带 |
| 主控板支撑柱 | 4 | 铜柱/焊接螺柱 |

> 金属焊接（建议不锈钢或镀锌钢）：养殖场高湿、氨气、粉尘腐蚀环境，直接金属焊接，不做 3D 打印。平台板 8 个 M4 孔（孔径 4.5mm，孔位见 3.2 和巡检机器人平台板.scad）按图纸加工。
